interface:
  display_name: "Delegate Team"
  short_description: "Repo-specific patterns and workflows for delegate-team"
  default_prompt: "Use the delegate-team repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true